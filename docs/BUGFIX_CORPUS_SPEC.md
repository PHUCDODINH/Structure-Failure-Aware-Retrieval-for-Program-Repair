# Bug-Fix Corpus Spec

This project currently expects retrieval records with `buggy_code` and `fixed_code`.
For a defensible external bug-fix corpus, use the following schema and filters.

## Goals

- Use real Python bug-fix pairs.
- Avoid benchmark leakage into HumanEval, MBPP, and QuixBugs evaluation.
- Prefer small, localized code fixes that are useful for prompting.

## Normalized Schema

Every record should be normalized into this logical shape before indexing:

```json
{
  "id": "bugsinpy:pandas:123:file.py:function_name",
  "source_dataset": "bugsinpy",
  "source_split": "train",
  "language": "python",
  "task_family": "general_python",
  "repository": "pandas-dev/pandas",
  "instance_id": "bugsinpy_pandas_123",
  "file_path": "pandas/core/frame.py",
  "function_name": "to_dict",
  "buggy_code": "def ...",
  "fixed_code": "def ...",
  "prompt": "",
  "bug_type": "unknown",
  "failure_signal": "pytest test_frame.py::test_to_dict failed before fix",
  "tests": ["pytest test_frame.py::test_to_dict"],
  "commit_buggy": "abc123",
  "commit_fixed": "def456",
  "metadata": {
    "scope": "function",
    "files_changed": 1,
    "changed_lines": 12,
    "has_repro": true,
    "synthetic": false,
    "generated": false,
    "framework_heavy": false,
    "non_code_fix_only": false
  }
}
```

## Minimum Required Fields

- `id`
- `source_dataset`
- `language`
- `repository`
- `instance_id`
- `buggy_code`
- `fixed_code`

## Hard Inclusion Rules

- Language must be Python.
- Buggy and fixed code must both exist.
- Buggy and fixed code must differ.
- Fix must be reproducible or validated by tests, failing/passing commit states, or equivalent evidence.
- Scope should be one of:
  - `function`
  - `method`
  - `small_module`

## Hard Exclusion Rules

- Synthetic or autogenerated mutations.
- Records derived from MBPP, MBPP synthetic bugs, QuixBugs, or HumanEval.
- Non-code fixes only.
- Environment-only, build-only, or dependency-only fixes.
- Multi-file refactors with `files_changed > 3`.
- Large patches with `changed_lines > 80`.

## Benchmark-Specific Retrieval Rules

### HumanEval

- Allowed task families:
  - `algorithmic_python`
  - `general_python`
- Prefer function-level examples.

### MBPP

- Exclude all MBPP-derived and MBPP-synthetic records.
- Prefer function-level algorithmic or utility-style Python fixes.

### QuixBugs

- Exclude all QuixBugs-derived records.
- Prefer algorithmic Python fixes.

## Recommended Source Priority

1. BugsInPy
2. PyBugHive
3. BugSwarm, Python-only filtered subset
4. FixEval as an auxiliary corpus only

## Why This Matters

Your current merged corpus contains:

- QuixBugs bug-fix pairs
- MBPP synthetic bug-fix pairs derived from MBPP eval tasks

That causes leakage for MBPP and QuixBugs evaluation. A clean external corpus should remove benchmark-derived records entirely.

## Conversion Target

After normalization, records can be written back into the current retrieval JSONL shape:

```json
{
  "id": "...",
  "source": "bugsinpy",
  "buggy_code": "...",
  "fixed_code": "...",
  "bug_type": "unknown",
  "prompt": "",
  "tests": [],
  "source_split": "train",
  "language": "python",
  "task_family": "general_python",
  "repository": "owner/repo",
  "instance_id": "bugsinpy_repo_123",
  "file_path": "path/to/file.py",
  "function_name": "name",
  "failure_signal": "...",
  "commit_buggy": "...",
  "commit_fixed": "...",
  "metadata": {}
}
```

This keeps the current retrievers working while making the corpus defensible.
